{
  "name": "shiftbox",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:website": "pnpm --filter @shiftbox/website dev",
    "build:website": "pnpm --filter @shiftbox/website build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "fmt": "pnpm -r format"
  }
}
